<!DOCTYPE html>
<html lang="es">
<head>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1" />
	<title>css en cabecera</title>
	<style>
		:root { --bg:#f7f9ff; --brand:#2563eb; --panel:#ffffff; --line:#e5e7eb; --muted:#64748b; }
		* { box-sizing: border-box; }
		body { margin:0; font-family:system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, 'Helvetica Neue', Arial, sans-serif; background:var(--bg); color:#1f2937; }
		header { background:#eaf2ff; padding:24px; border-bottom:1px solid #dbeafe; }
		nav { display:flex; gap:12px; padding:12px 24px; background:#eff6ff; border-bottom:1px solid #dbeafe; }
		nav a { text-decoration:none; padding:8px 12px; border-radius:10px; background:var(--panel); border:1px solid var(--line); }
		main { display:grid; grid-template-columns:1fr 320px; gap:24px; padding:24px; max-width:1100px; margin:0 auto; }
		section, aside { background:var(--panel); border:1px solid var(--line); border-radius:16px; padding:20px; }
		h1,h2,h3 { margin-top:0; }
		article { padding:12px; border-bottom:1px dashed var(--line); }
		button { padding:8px 12px; border-radius:10px; border:1px solid #d1d5db; background:#f9fafb; cursor:pointer; }
		form { margin-top:16px; padding:12px; background:#f8fafc; border:1px solid #e2e8f0; border-radius:12px; }
		input[type="email"] { width:100%; padding:10px; border:1px solid #cbd5e1; border-radius:10px; }
		figure { margin:16px 0 0 0; }
		img { width:100%; border-radius:12px; border:1px solid var(--line); }
		figcaption { font-size:14px; color:var(--muted); }
		footer { text-align:center; padding:16px; color:var(--muted); }
		@media (max-width: 900px) { main { grid-template-columns: 1fr; } }
	</style>
</head>
<body>
	<a href="#contenido" class="skip">Saltar al contenido</a>
	<header>
		<h1>Café Andino</h1>
		<p>Sabores de altura para tu día</p>
	</header>
	<nav aria-label="principal">
		<a href="#menu">Menú</a>
		<a href="#locales">Locales</a>
		<a href="#contacto">Contacto</a>
	</nav>
	<main id="contenido">
		<section id="menu">
			<h2>Nuestro menú</h2>
			<article>
				<h3>Capuchino</h3>
				<p>Espuma cremosa y aroma intenso.</p>
				<button type="button">Agregar</button>
			</article>
			<article>
				<h3>Americano</h3>
				<p>Suave y equilibrado.</p>
				<button type="button">Agregar</button>
			</article>
			<form aria-labelledby="suscripcion">
				<h3 id="suscripcion">Suscríbete</h3>
				<label for="email">Correo electrónico</label>
				<input id="email" name="email" type="email" required placeholder="tucorreo@ejemplo.com" />
				<button>Enviar</button>
			</form>
		</section>
		<aside id="locales">
			<h2>Locales</h2>
			<ul>
				<li>Arequipa Centro</li>
				<li>Cayma</li>
				<li>Yanahuara</li>
			</ul>
			<figure>
				<img src="https://picsum.photos/seed/cafe/640/360" alt="Taza de café sobre mesa" />
				<figcaption>Ambiente cálido y moderno</figcaption>
			</figure>
		</aside>
	</main>
	<footer>© 2025 Café Andino</footer>
</body>
</html>
